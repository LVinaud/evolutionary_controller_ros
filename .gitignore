__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# colcon artifacts (in case it's run from the package dir by mistake)
build/
install/
log/

# IDE
.vscode/
.idea/
*.swp

# Training logs and intermediate genomes — commit only the champion
logs/
genomes/*
!genomes/.gitkeep
!genomes/best.npy

# Local config files containing tokens / IPs — never commit
config/redis.env
config/workers.yaml
